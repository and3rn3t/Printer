# ──────────────────────────────────────────────────────────────
# Printer — iOS CI
# Builds, lints, and tests the Printer app on every push / PR.
# ──────────────────────────────────────────────────────────────

name: iOS CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - '.github/copilot-instructions.md'
      - 'LICENSE'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**.md'
      - '.github/copilot-instructions.md'
      - 'LICENSE'
  workflow_dispatch: # allow manual trigger

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  SCHEME: Printer
  PROJECT: Printer.xcodeproj
  XCBEAUTIFY_OPTS: "--disable-colored-output"

jobs:
  # ── 1. SwiftLint ──────────────────────────────────────────
  lint:
    name: SwiftLint
    runs-on: macos-15
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Lint
        run: swiftlint lint --strict --config .swiftlint.yml --reporter github-actions-logging

  # ── 2. Build ──────────────────────────────────────────────
  build:
    name: Build (iOS)
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          # Use the latest available Xcode on the runner
          XCODE_PATH=$(ls -d /Applications/Xcode*.app 2>/dev/null | sort -V | tail -1)
          echo "Using Xcode at: $XCODE_PATH"
          sudo xcode-select -s "$XCODE_PATH"
          xcodebuild -version
          xcrun simctl list runtimes

      - name: Resolve simulator destination
        run: |
          # Pick the best available iPhone simulator
          DEST=$(xcrun simctl list devices available -j | python3 -c "
          import json, sys
          data = json.load(sys.stdin)
          # Prefer iPhone 16, then any iPhone, sorted by runtime version
          candidates = []
          for runtime, devices in data['devices'].items():
              for d in devices:
                  if 'iPhone' in d['name'] and d['isAvailable']:
                      candidates.append((runtime, d['name']))
          candidates.sort(key=lambda x: x[0], reverse=True)
          for runtime, name in candidates:
              if 'iPhone 16' in name:
                  print(name); sys.exit(0)
          for runtime, name in candidates:
              if 'iPhone' in name:
                  print(name); sys.exit(0)
          sys.exit(1)
          ")
          echo "Resolved simulator: $DEST"
          echo "DESTINATION=platform=iOS Simulator,name=$DEST,OS=latest" >> "$GITHUB_ENV"

      - name: Install xcbeautify
        run: brew install xcbeautify

      - name: Resolve package dependencies
        run: |
          xcodebuild -project "$PROJECT" \
            -scheme "$SCHEME" \
            -resolvePackageDependencies \
            2>&1 | xcbeautify $XCBEAUTIFY_OPTS

      - name: Build for iOS Simulator
        run: |
          set -o pipefail
          xcodebuild build \
            -project "$PROJECT" \
            -scheme "$SCHEME" \
            -destination "$DESTINATION" \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            COMPILER_INDEX_STORE_ENABLE=NO \
            2>&1 | xcbeautify $XCBEAUTIFY_OPTS

  # ── 3. Test ───────────────────────────────────────────────
  test:
    name: Test (iOS)
    runs-on: macos-15
    needs: build
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          XCODE_PATH=$(ls -d /Applications/Xcode*.app 2>/dev/null | sort -V | tail -1)
          sudo xcode-select -s "$XCODE_PATH"
          xcodebuild -version

      - name: Resolve simulator destination
        run: |
          DEST=$(xcrun simctl list devices available -j | python3 -c "
          import json, sys
          data = json.load(sys.stdin)
          candidates = []
          for runtime, devices in data['devices'].items():
              for d in devices:
                  if 'iPhone' in d['name'] and d['isAvailable']:
                      candidates.append((runtime, d['name']))
          candidates.sort(key=lambda x: x[0], reverse=True)
          for runtime, name in candidates:
              if 'iPhone 16' in name:
                  print(name); sys.exit(0)
          for runtime, name in candidates:
              if 'iPhone' in name:
                  print(name); sys.exit(0)
          sys.exit(1)
          ")
          echo "Resolved simulator: $DEST"
          echo "DESTINATION=platform=iOS Simulator,name=$DEST,OS=latest" >> "$GITHUB_ENV"

      - name: Install xcbeautify
        run: brew install xcbeautify

      - name: Run unit tests
        run: |
          set -o pipefail
          xcodebuild test \
            -project "$PROJECT" \
            -scheme "$SCHEME" \
            -destination "$DESTINATION" \
            -configuration Debug \
            -resultBundlePath TestResults.xcresult \
            -enableCodeCoverage YES \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            COMPILER_INDEX_STORE_ENABLE=NO \
            2>&1 | xcbeautify $XCBEAUTIFY_OPTS

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: TestResults
          path: TestResults.xcresult
          retention-days: 14
          if-no-files-found: warn

  # ── 4. Build Widget Extension ─────────────────────────────
  build-widget:
    name: Build Widget Extension
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          XCODE_PATH=$(ls -d /Applications/Xcode*.app 2>/dev/null | sort -V | tail -1)
          sudo xcode-select -s "$XCODE_PATH"

      - name: Resolve simulator destination
        run: |
          DEST=$(xcrun simctl list devices available -j | python3 -c "
          import json, sys
          data = json.load(sys.stdin)
          candidates = []
          for runtime, devices in data['devices'].items():
              for d in devices:
                  if 'iPhone' in d['name'] and d['isAvailable']:
                      candidates.append((runtime, d['name']))
          candidates.sort(key=lambda x: x[0], reverse=True)
          for runtime, name in candidates:
              if 'iPhone 16' in name:
                  print(name); sys.exit(0)
          for runtime, name in candidates:
              if 'iPhone' in name:
                  print(name); sys.exit(0)
          sys.exit(1)
          ")
          echo "Resolved simulator: $DEST"
          echo "DESTINATION=platform=iOS Simulator,name=$DEST,OS=latest" >> "$GITHUB_ENV"

      - name: Install xcbeautify
        run: brew install xcbeautify

      - name: Build widget extension
        run: |
          set -o pipefail
          xcodebuild build \
            -project "$PROJECT" \
            -scheme widgetExtension \
            -destination "$DESTINATION" \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            COMPILER_INDEX_STORE_ENABLE=NO \
            2>&1 | xcbeautify $XCBEAUTIFY_OPTS
